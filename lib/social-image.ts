import { SITE_URL } from "@/lib/site";

/** Actual hero dimensions; a new URL also avoids reusing the old solid-color card. */
export const DEFAULT_SHARE_IMAGE = {
  url: `${SITE_URL}/images/home/seoul-neighborhood-hero.png`,
  width: 1672,
  height: 941,
  alt: "A Drop of Seoul — everyday Korean life, seen from a Seoul neighborhood",
};
